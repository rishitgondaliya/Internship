<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="assets/shopping-cart.png">
    <title><%= pageTitle %> </title>
    <link rel="stylesheet" href="../../style.css" />
  </head>
  <body>
    <header class="main-header">
      <nav class="nav">
        <ul class="ul">
          <li class="item"><a class="<%= path === '/' ? 'active' : '' %>" href="/">Shop</a></li>
          <li class="item"><a class="<%= path === '/products' ? 'active' : '' %>" href="/products">Products</a></li>
          <% if (isAuthenticated) { %>
            <li class="item"><a class="<%= path === '/cart' ? 'active' : '' %>" href="/cart">View Cart</a></li>
            <li class="item"><a class="<%= path === '/orders' ? 'active' : '' %>" href="/orders">Orders</a></li>
            <li class="item"><a class="<%= path === '/admin/add-product' ? 'active' : '' %>" href="/admin/add-product">Add Product</a></li>
            <li class="item"><a class="<%= path === '/admin/products' || path === '/admin/edit-product'  ? 'active' : '' %>" href="/admin/products">Admin Products</a></li>
          <% } %>
        </ul>
        <ul class="ul">
            <% if (!isAuthenticated) { %>
              <li class="item">
                  <a href="/login" class="auth-btn <%= path === '/login' ? 'active' : '' %>">Login</a>
              </li>
              <li class="item">
                <a href="/signup" class="auth-btn <%= path === '/signup' ? 'active' : '' %>">Signup</a>
              </li>
            <% } else { %>
              <li class="item">
                  <form action="/logout" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                     <button type="submit" class="auth-btn logout-btn <%= path === '/logout' ? 'active' : '' %>">Logout</button>
                  </form>
              </li>
            <% } %>
          </ul>
      </nav>
    </header>