<%- include('includes/main.ejs') %>
    <h1>Users :</h1>
    <% if (users.length > 0) { %>
    <ul>
      <% users.forEach((user) => { %>
      <li><%= user %></li>
      <% }) %>
    </ul>
    <% } else { %>
    <h2>No users found !</h2>
    <% } %>
  </body>
</html>
